{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "title": "PR Update executor",
  "description": "Update release PR description with AI summary",
  "type": "object",
  "properties": {
    "baseBranch": {
      "type": "string",
      "description": "The base branch (source of PR)",
      "default": "main"
    },
    "targetBranch": {
      "type": "string",
      "description": "The target branch (destination of PR)",
      "default": "production"
    },
    "aiCommand": {
      "type": "string",
      "description": "Command to run for AI summary generation (should accept prompt via stdin). Leave empty to skip AI summaries."
    },
    "aiTitleCommand": {
      "type": "string",
      "description": "Command to run for AI title generation (should accept prompt via stdin). Defaults to aiCommand if not specified."
    },
    "model": {
      "type": "string",
      "description": "AI model to use (overrides model in aiCommand if specified). Examples: fast, gpt-4o, sonnet, etc."
    },
    "maxLinesPerFile": {
      "type": "number",
      "description": "Maximum lines per file in diff sent to AI (prevents large files from dominating)",
      "default": 150
    },
    "interactiveAI": {
      "type": "boolean",
      "description": "Use interactive AI mode where AI selects which files to examine (more efficient for large PRs)",
      "default": false
    },
    "verbose": {
      "type": "boolean",
      "description": "Enable verbose logging for debugging",
      "default": false
    },
    "diffPaths": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Paths to include in diff for AI analysis",
      "default": ["packages/*/src/**", "packages/connectors/*/src/**"]
    },
    "changelogPatterns": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Glob patterns for changelog files",
      "default": [
        "packages/*/CHANGELOG.md",
        "packages/connectors/*/CHANGELOG.md"
      ]
    }
  }
}
